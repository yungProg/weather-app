# my-webpack
This repository is template with basic files and configurations for my node.js projects

## important!

run `npm install --save-dev webpack-merge` to install required modules because git isn't tracking node_modules dir.
